# runs 'brew shellenv'
#   adds brew to multiple paths and exports multiple variables
#   see "'man brew' |> '/shellenv'" for more details
eval "$(/opt/homebrew/bin/brew shellenv)"
#eval "$(brew shellenv)"

. /opt/homebrew/opt/asdf/libexec/asdf.sh

. "$HOME/.rye/env"
. "$HOME/.cargo/env"
